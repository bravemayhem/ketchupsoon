ALTER TABLE event_attendees DROP CONSTRAINT IF EXISTS event_attendees_attendee_id_fkey; ALTER TABLE event_attendees DROP COLUMN IF EXISTS attendee_id; ALTER TABLE event_attendees ADD COLUMN IF NOT EXISTS name TEXT NOT NULL; ALTER TABLE event_attendees ADD COLUMN IF NOT EXISTS email TEXT NOT NULL; ALTER TABLE event_attendees ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'yes'; ALTER TABLE event_attendees ALTER COLUMN status SET DEFAULT 'yes'; DROP POLICY IF EXISTS "Enable read access for all users" ON event_attendees; CREATE POLICY "Enable read access for all users" ON event_attendees FOR SELECT USING (true); DROP POLICY IF EXISTS "Enable insert access for all users" ON event_attendees; CREATE POLICY "Enable insert access for all users" ON event_attendees FOR INSERT WITH CHECK (true);
